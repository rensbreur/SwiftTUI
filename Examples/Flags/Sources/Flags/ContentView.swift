import SwiftTUI

struct ContentView: View {
    var body: some View {
        HStack {
            Spacer()
            VStack {
                Spacer()
                FlagEditor()
                Spacer()
            }
            Spacer()
        }
    }
}
